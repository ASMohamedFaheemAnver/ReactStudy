import Memo from "./problems/Memo";

function App() {
  return (
    <div
      style={{
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <Memo />
    </div>
  );
}

export default App;
